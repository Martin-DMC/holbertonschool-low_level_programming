/**
 * main - imprime un mensaje en pantalla
 * Description: escribe en pantalla usando el comando printf
 * Return: retorna 0
 */
#include <stdio.h>

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
