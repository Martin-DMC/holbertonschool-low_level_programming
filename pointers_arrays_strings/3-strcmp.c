/**
 * _strcmp - compara cadenas
 * @s1: cadena 1
 * @s2: cadena 2
 *
 * Return: retorna valor
 */

int _strcmp(char *s1, char *s2)
{
	int i = 0;

	while (s1[i] != '\0' && s2[i] != '\0')
	{
		if (s1[i] != s2[i])
		{
			return (s1[i] - s2[i]);
		}
		i++;
	}
	return (s1[i] - s2[i]);
}
