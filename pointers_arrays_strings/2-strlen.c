/**
 * _strlen - cuenta caracteres
 * @s: cadena
 *
 * Description: cuenta una cadena de caracteres
 *
 * Return: cantidad de caracteres
 */
#include <string.h>

int _strlen(char *s)
{
	int r = strlen(s);

	return (r);
}
