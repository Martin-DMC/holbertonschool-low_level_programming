/**
 * main - imprime por pantalla
 * Description: imprime una cadena de numeros por pantalla
 * Return: retorna 0
 */
#include <stdio.h>

int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		i++;
	}
	return (0);
}
